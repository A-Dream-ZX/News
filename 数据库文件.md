```sql
-- 创建评论表 `comments`
CREATE TABLE `comments` (
  `cid` int NOT NULL AUTO_INCREMENT, -- cid: 评论的唯一标识符，主键，自动递增
  `cnid` int NOT NULL,               -- cnid: 对应新闻文章的ID，外键（未在本语句中定义）
  `ccontent` varchar(3000) NOT NULL, -- ccontent: 评论内容，最大长度为3000个字符
  `cdate` datetime DEFAULT NULL,     -- cdate: 评论的时间，默认值为空
  `cip` varchar(100) NOT NULL,       -- cip: 发布评论时用户的IP地址
  `cauthor` varchar(100) NOT NULL,   -- cauthor: 发布评论的作者的名字
  PRIMARY KEY (`cid`) USING BTREE     -- 定义主键索引使用B树结构
);
```



```sql
-- 创建新闻表 `news`
CREATE TABLE `news` (
  `nid` int NOT NULL AUTO_INCREMENT, -- nid: 新闻文章的唯一标识符，主键，自动递增
  `ntid` int NOT NULL,               -- ntid: 新闻所属主题的ID，外键（未在本语句中定义）
  `ntitle` varchar(200) NOT NULL,    -- ntitle: 新闻标题
  `nauthor` varchar(50) DEFAULT NULL,-- nauthor: 新闻作者的名字，默认值为空
  `ncreatedate` datetime NOT NULL,   -- ncreatedate: 新闻创建的时间
  `npicpath` varchar(500) DEFAULT NULL, -- npicpath: 新闻图片的路径，默认值为空
  `ncontent` varchar(4000) NOT NULL, -- ncontent: 新闻内容的最大长度为4000个字符
  `nmodifydate` datetime DEFAULT NULL, -- nmodifydate: 最后修改新闻的时间，默认值为空
  `nsummary` varchar(2000) NOT NULL, -- nsummary: 新闻摘要，最大长度为2000个字符
  PRIMARY KEY (`nid`) USING BTREE     -- 定义主键索引使用B树结构
);
```

```sql
-- 创建用户表 `news_users`
CREATE TABLE `news_users` (
  `usid` int NOT NULL AUTO_INCREMENT, -- usid: 用户的唯一标识符，主键，自动递增
  `uname` varchar(20) NOT NULL,       -- uname: 用户名，最大长度为20个字符
  `upwd` varchar(20) NOT NULL,        -- upwd: 用户密码，最大长度为20个字符
  PRIMARY KEY (`usid`) USING BTREE     -- 定义主键索引使用B树结构
);
```



```sql
-- 创建主题表 `topic`
CREATE TABLE `topic` (
  `tid` int NOT NULL AUTO_INCREMENT, -- tid: 主题的唯一标识符，主键，自动递增
  `tname` varchar(50) NOT NULL,      -- tname: 主题名称，最大长度为50个字符
  PRIMARY KEY (`tid`) USING BTREE     -- 定义主键索引使用B树结构
);
```



comments 表：
该表存储了与新闻相关的评论信息。

cid 	是评论的唯一标识符，自动递增。
cnid 	是外键，关联到 news 表中的 nid 字段，表示这条评论属于哪条新闻。
ccontent 存储评论的内容，最大长度为3000个字符。
cdate 记录评论的时间。
cip 存储发表评论者的IP地址。
cauthor 存储评论作者的名字。

news 表：
此表包含新闻文章的信息。
nid 是每篇新闻文章的唯一标识符，自动递增。
ntid 可能是外键，关联到 topic 表中的 tid 字段，表明这篇新闻属于哪个话题或类别。
ntitle, nauthor, ncreatedate, npicpath, ncontent, nmodifydate, nsummary 分别存储新闻标题、作者、创建日期、图片路径、内容、最后修改日期和摘要。

news_users 表：
该表管理系统的用户信息。
usid 是用户的唯一标识符，自动递增。
uname 和 upwd 分别存储用户名和密码。

topic 表：
这个表定义了新闻文章可以归属的话题或类别。
tid 是话题的唯一标识符，自动递增。
tname 存储话题的名字。





